import * as ReactDOM from 'react-dom';
import * as React from "react";

const ROOT_ELEMENT = document.getElementById('calculator-toning');

//получение урла откуда грузить ресурсы 3d модельки
console.log(ROOT_ELEMENT.dataset.resources);

ReactDOM.render(
	<div>batya2</div>,
	ROOT_ELEMENT
);
